#home-title {
    font-size: clamp(1.5rem, 5vw, 3rem); /* Ajusta el tamaño según la pantalla */
    word-wrap: break-word;               /* Asegura que las palabras largas no se corten */
    text-align: center;                  /* Centra el texto */
}
